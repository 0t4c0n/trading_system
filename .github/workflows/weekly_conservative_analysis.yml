# .github/workflows/weekly_conservative_analysis.yml
name: Enhanced Conservative Stock Analysis with Trading Levels

on:
  schedule:
    # Sábados a las 10:00 AM España (9:00 AM UTC)
    - cron: '0 9 * * 6'
  workflow_dispatch:  # Permitir ejecución manual

# PERMISOS NECESARIOS PARA GITHUB PAGES Y COMMITS
permissions:
  contents: write
  pages: write
  id-token: write

# Asegurar que solo un workflow corra a la vez
concurrency:
  group: "enhanced-conservative-analysis"
  cancel-in-progress: false

jobs:
  enhanced-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 150  # 2.5 horas para análisis completo
    
    # Configurar entorno para GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Historial completo para análisis de consistencia
        
    - name: Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Cache dependencies mejorado
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-enhanced-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-enhanced-
          ${{ runner.os }}-pip-
        
    - name: Instalar dependencias
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Verificar archivos de configuración y estado del historial
      run: |
        echo "=== VERIFICANDO CONFIGURACION MEJORADA CON HISTORIAL ==="
        if [ ! -f "current_portfolio.json" ]; then
          echo "ERROR: current_portfolio.json no encontrado"
          exit 1
        fi
        echo "✅ Configuración verificada"
        
        # Verificar estado del historial
        if [ -d "historical_data" ]; then
          hist_count=$(ls historical_data/ 2>/dev/null | wc -l)
          echo "📚 Archivos históricos disponibles: $hist_count"
        else
          echo "ℹ️  Primera ejecución - se creará historial automáticamente"
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: Configurar variables de entorno para análisis mejorado
      run: |
        echo "REPORT_DATE=$(date +%Y-%m-%d)" >> $GITHUB_ENV
        echo "ANALYSIS_START_TIME=$(date -u +%H:%M)" >> $GITHUB_ENV
        echo "ENHANCED_MODE=true" >> $GITHUB_ENV
        echo "AUTO_HISTORY=true" >> $GITHUB_ENV
        
    - name: 1. Screening conservador mejorado con historial automático
      run: |
        echo "STEP 1: Screening completo 3,000+ acciones con gestión automática de historial..."
        echo "El sistema archivará automáticamente resultados anteriores (si existen)"
        echo "Tiempo estimado: 15-25 minutos para análisis completo"
        
        python conservative_screener.py
        
        if [ -f "weekly_screening_results.json" ]; then
          echo "SUCCESS: Screening completado con gestión automática de historial"
          # Verificar que el archivo contiene datos válidos
          python -c "import json; data=json.load(open('weekly_screening_results.json')); print(f'Resultados: {len(data.get(\"detailed_results\", []))}')"
        else
          echo "ERROR: Error en screening conservador"
          exit 1
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: 2. Analizar consistencia histórica con archivos mejorados
      run: |
        echo "STEP 2: Análisis de consistencia (5 semanas) con gestión de historial..."
        echo "El sistema archivará automáticamente el análisis anterior (si existe)"
        echo "Buscará archivos históricos en múltiples formatos para máxima compatibilidad"
        
        python consistency_analyzer.py
        
        if [ -f "consistency_analysis.json" ]; then
          echo "SUCCESS: Análisis de consistencia completado con gestión de historial"
          python verify_consistency.py
        else
          echo "ERROR: Error en análisis de consistencia"
          exit 1
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: 3. Generar recomendaciones de rotación con historial
      run: |
        echo "STEP 3: Recomendaciones de rotación con análisis multifactorial y gestión de historial..."
        echo "El sistema archivará automáticamente las recomendaciones anteriores (si existen)"
        
        python rotation_recommender.py
        
        if [ -f "rotation_recommendations.json" ]; then
          echo "SUCCESS: Recomendaciones de rotación completadas con gestión de historial"
          python verify_rotation.py
        else
          echo "ERROR: Error en recomendaciones de rotación"
          exit 1
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: 4. Crear reporte semanal mejorado con historial
      run: |
        echo "STEP 4: Generando reporte semanal mejorado con niveles de trading y gestión de historial..."
        echo "El sistema gestionará automáticamente el historial de reportes"
        
        python create_weekly_report.py
        
        echo "Verificando reportes generados..."
        if [ -f "docs/data.json" ]; then
          echo "SUCCESS: Dashboard data.json generado"
          python verify_dashboard.py
        else
          echo "ERROR: Error generando dashboard data"
          exit 1
        fi
        
        # Verificar reportes Markdown
        ENHANCED_REPORT=$(ls ENHANCED_WEEKLY_REPORT_*.md 2>/dev/null | head -1)
        REGULAR_REPORT=$(ls WEEKLY_REPORT_*.md 2>/dev/null | head -1)
        
        if [ -n "$ENHANCED_REPORT" ]; then
          echo "SUCCESS: Reporte mejorado generado: $ENHANCED_REPORT"
        elif [ -n "$REGULAR_REPORT" ]; then
          echo "SUCCESS: Reporte regular generado: $REGULAR_REPORT"
        else
          echo "WARNING: No se generó reporte Markdown (puede ser normal)"
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: Verificar archivos generados y calidad con historial
      run: |
        python verify_generated_files.py
      env:
        PYTHONUNBUFFERED: 1
        
    - name: Configurar Git para commits automáticos
      run: |
        git config --global user.name 'Enhanced Conservative Bot'
        git config --global user.email 'actions@github.com'
        
    - name: Generar mensaje de commit inteligente
      run: |
        python generate_commit_message.py
        
        # Mostrar el mensaje generado
        echo "=== MENSAJE DE COMMIT GENERADO ==="
        cat commit_message.txt
        echo "================================="
      env:
        PYTHONUNBUFFERED: 1
        
    - name: Commit y push de resultados con historial automático
      run: |
        # Verificar si hay cambios
        if git diff --quiet && git diff --staged --quiet; then
          echo "Sin cambios para commitear"
        else
          # Añadir archivos principales
          git add weekly_screening_results.json
          git add consistency_analysis.json  
          git add rotation_recommendations.json
          git add docs/data.json
          
          # Añadir reportes si existen
          git add ENHANCED_WEEKLY_REPORT_*.md 2>/dev/null || true
          git add WEEKLY_REPORT_*.md 2>/dev/null || true
          
          # Añadir historial si existe
          git add historical_data/ 2>/dev/null || true
          
          # Commit con mensaje personalizado
          if [ -f "commit_message.txt" ]; then
            git commit -F commit_message.txt
          else
            git commit -m "Enhanced Weekly Analysis $(date +%Y-%m-%d) - Auto History"
          fi
          
          git push
          echo "SUCCESS: Resultados commiteados con historial automático"
        fi
        
    - name: Configurar GitHub Pages
      uses: actions/configure-pages@v5
      
    - name: Subir artefactos para Pages
      uses: actions/upload-pages-artifact@v1
      with:
        path: './docs'
        
    - name: Desplegar a GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v3
      
    - name: Resumen final con historial y métricas
      run: |
        echo "🎉 ===== ANÁLISIS MEJORADO COMPLETADO CON ÉXITO ====="
        echo "🕐 Iniciado: $ANALYSIS_START_TIME UTC"
        echo "🕐 Finalizado: $(date -u +%H:%M) UTC"
        echo ""
        echo "📊 ARCHIVOS GENERADOS:"
        ls -la *.json *.md 2>/dev/null || echo "Sin archivos markdown"
        echo ""
        echo "🌐 DASHBOARD: ${{ steps.deployment.outputs.page_url }}"
        echo ""
        echo "📚 GESTIÓN DE HISTORIAL:"
        if [ -d "historical_data" ]; then
          echo "✅ Historial automático funcionando"
          echo "📁 Archivos históricos: $(ls historical_data/ 2>/dev/null | wc -l)"
        else
          echo "ℹ️  Historial se creará en próximas ejecuciones"
        fi
        echo ""
        echo "🔄 Próxima ejecución: Sábados 10:00 AM España"
        echo "⚡ Ejecución manual disponible en Actions tab"
        echo "=============================================="